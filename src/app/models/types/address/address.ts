export type Address = {
  addressId: string;
  city: string;
  country: string;
  postalCode: string;
  addressDefault: boolean;
  bothAddressesDefault: boolean;
  streetName: string;
  streetNumber: string;
};
